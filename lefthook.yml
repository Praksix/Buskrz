
      # Hook qui s'exécute AVANT le commit
pre-commit:
  commands:
    # Lint du frontend React
    frontend-lint:
      run: cd client/buskrz-front && npm run lint
      
    # Tests et compilation du backend Java
    #backend-test:
      #run: cd api && ./mvnw test
      
    # Compilation du backend Java
    #backend-compile:
      #run: cd api && ./mvnw compile
      
# Hook qui s'exécute APRÈS le commit réussi
post-commit:
  commands:
    # Message de confirmation
    celebrate:
      run: echo "✅ Commit validé avec succès!"
      
# Hook qui s'exécute AVANT le push
pre-push:
  commands:
    # Build complet du frontend
    frontend-build:
      run: cd client/buskrz-front && npm run build
      
    # Tests complets du backend
    #backend-full-test:
      #run: cd api && ./mvnw test